#ifndef PROTENCRIPTAR_H_INCLUDED
#define PROTENCRIPTAR_H_INCLUDED

#define isEmpty(X) ((X) == ' ' || (X) == '\t')
#define TAM_ROW 1
#define TAM_COLUM 5

char *encryptingString(char *s);
char encrypt(const char c);

#endif // PROTENCRIPTAR_H_INCLUDED
